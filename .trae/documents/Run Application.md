I will start the development server so you can view and interact with the app.

**Action:**

1. Execute `npx expo start` in the terminal.
2. Provide the `localhost` preview URL once the server is running.

